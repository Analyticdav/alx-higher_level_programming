#!/usr/bin/python3

def element_at(my_list, element):

    list_length = len(my_list)

    if (idx < 0 or idx >= list_length):

        return (my_list)

    else:

        my_list[idx] = element

        return (my_list)
