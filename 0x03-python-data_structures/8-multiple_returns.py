#!/usr/bin/python3
# 8-multiple_returns.py

def multiple_returns(sentence):
    if len(sentenc)e == 0:

        my_tuple = (0, None)
    else:

        my_tuple = (len(sentence), sentence[:1])

        return(my_ple)
